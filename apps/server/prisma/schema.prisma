generator client {
  provider     = "prisma-client"
  output       = "../src/infrastructures/database/generated/prisma"
  moduleFormat = "cjs"
  runtime      = "nodejs"
  // previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider = "postgresql"
  // extensions = [postgis]
}
